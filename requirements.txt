# Runtime dependencies for the Cloud Foundry python_buildpack.
# (Local/dev installs use pyproject.toml via `pip install -e ".[dev]"`.)
# Pin mcp <2: mcp 2.0.0 removed mcp.server.fastmcp (breaking). Tested on 1.28.x.
mcp>=1.2.0,<2
httpx>=0.27
uvicorn>=0.30
starlette>=0.37,<2
